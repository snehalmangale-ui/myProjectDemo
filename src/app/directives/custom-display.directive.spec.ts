import { CustomDisplayDirective } from './custom-display.directive';

describe('CustomDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
